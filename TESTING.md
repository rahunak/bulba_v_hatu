# Инструкция по тестированию локально

## ✅ Текущий статус
- Приложение запущено в режиме polling (работает без webhook)
- База данных создана и содержит 5 товаров
- Боты готовы к тестированию

## 📋 Шаги для тестирования

### Шаг 1: Узнайте свой Telegram ID
1. Откройте Telegram
2. Найдите бота **@userinfobot**
3. Отправьте ему любое сообщение (например, "hi")
4. Скопируйте ваш **ID** (это число, например: 987654321)

### Шаг 2: Обновите админа в базе данных
Выполните команду, заменив `ваш_telegram_id` на реальный ID из Шага 1:

```bash
docker compose exec postgres psql -U postgres -d veggie_bot -c "UPDATE \"User\" SET \"telegramId\" = 'ваш_telegram_id' WHERE role = 'ADMIN';"
```

Проверьте, что обновление прошло успешно:
```bash
docker compose exec postgres psql -U postgres -d veggie_bot -c "SELECT \"telegramId\", username, role FROM \"User\" WHERE role = 'ADMIN';"
```

### Шаг 3: Перезапустите приложение
```bash
docker compose restart app
```

Подождите 3-5 секунд и проверьте логи:
```bash
docker compose logs app --tail 20
```

Вы должны увидеть: `Nest application successfully started`

---

## 🧪 Тестирование клиентского бота

### 1. Запуск бота
1. Откройте Telegram
2. Найдите вашего **клиентского бота** (имя, которое вы дали при создании)
3. Нажмите **Start** или отправьте `/start`

**Ожидаемый результат:**
- Приветственное сообщение: "🥬 Добро пожаловать в магазин овощей!"
- Клавиатура с кнопками:
  - 📦 Каталог товаров
  - 🛒 Моя корзина
  - 📋 Мои заказы

### 2. Просмотр каталога
1. Нажмите **"📦 Каталог товаров"**

**Ожидаемый результат:**
- Список из 5 товаров:
  - Помидоры (3.50 BYN)
  - Огурцы (2.80 BYN)
  - Картофель (1.50 BYN)
  - Морковь (2.00 BYN)
  - Капуста (2.50 BYN)
- Каждый товар с кнопкой "➕ Добавить в корзину"

### 3. Добавление товаров в корзину
1. Нажмите **"➕ Добавить в корзину"** под любым товаром
2. Повторите для нескольких товаров

**Ожидаемый результат:**
- После каждого нажатия: всплывающее уведомление "✅ Товар добавлен в корзину"

### 4. Просмотр корзины
1. Нажмите **"🛒 Моя корзина"**

**Ожидаемый результат:**
- Список добавленных товаров с количеством и суммой
- Общая сумма заказа
- Кнопки:
  - ✅ Оформить заказ
  - 🗑 Очистить корзину

### 5. Оформление заказа
1. Нажмите **"✅ Оформить заказ"**
2. Бот попросит поделиться номером телефона
3. Нажмите кнопку **"📱 Отправить номер"** (это специальная кнопка Telegram)
4. Бот попросит ввести адрес доставки
5. Введите адрес (например: "Минск, ул. Ленина, 10, кв. 5")

**Ожидаемый результат:**
- Сообщение: "✅ Ваш заказ #[id] принят!"
- Корзина очищена
- Возврат в главное меню

---

## 🔧 Тестирование админского бота

### 1. Открытие админ-панели
1. Откройте Telegram
2. Найдите вашего **админского бота**
3. Отправьте `/start`

**Ожидаемый результат:**
- Сообщение: "🔧 Админ-панель"
- Клавиатура с кнопками:
  - 📦 Управление товарами
  - 📋 Активные заказы

### 2. Получение уведомления о новом заказе
**Когда клиент оформляет заказ**, админ-бот автоматически получит уведомление:

**Ожидаемый результат:**
```
🔔 Новый заказ #[id]

👤 Клиент: [username]
📞 Телефон: [номер]
📍 Адрес: [адрес доставки]

📦 Товары:
- Помидоры x2 = 7.0 BYN
- Огурцы x1 = 2.8 BYN

💰 Итого: 9.8 BYN
```

С кнопками:
- ✅ Принять
- ❌ Отменить

### 3. Управление заказом
1. Нажмите **"✅ Принять"** или **"❌ Отменить"**

**Ожидаемый результат:**
- Статус заказа изменится
- Клиент получит уведомление об изменении статуса
- Кнопки исчезнут

### 4. Просмотр активных заказов
1. Нажмите **"📋 Активные заказы"**

**Ожидаемый результат:**
- Список всех заказов со статусом PENDING или ACCEPTED
- Для каждого заказа кнопки управления

### 5. Просмотр товаров на складе
1. Отправьте команду `/stock`

**Ожидаемый результат:**
```
📦 Товары на складе:

✅ Помидоры
💰 Цена: 3.50 BYN
📦 Остаток: 100 шт.

✅ Огурцы
💰 Цена: 2.80 BYN
📦 Остаток: 150 шт.

[и т.д. для всех товаров]
```

### 6. Управление товарами
1. Нажмите **"📦 Управление товарами"**

**Ожидаемый результат:**
- Список всех товаров с кнопками:
  - ➕ Добавить остаток
  - ➖ Убавить остаток
  - ✅ Активировать / ❌ Деактивировать

---

## 🐛 Проверка логов

Если что-то не работает, проверьте логи:

```bash
# Логи приложения
docker compose logs app -f

# Логи базы данных
docker compose logs postgres

# Проверка статуса контейнеров
docker compose ps
```

---

## 📊 Проверка базы данных

Откройте Prisma Studio для визуального просмотра данных:

```bash
docker compose exec app npx prisma studio
```

Откройте в браузере: http://localhost:5555

Здесь вы можете:
- Просмотреть все заказы
- Проверить остатки товаров
- Увидеть сессии пользователей

---

## ✅ Чек-лист успешного тестирования

- [ ] Клиентский бот отвечает на /start
- [ ] Каталог товаров отображается
- [ ] Товары добавляются в корзину
- [ ] Корзина показывает правильную сумму
- [ ] Можно оформить заказ с телефоном и адресом
- [ ] Админ-бот получает уведомление о новом заказе
- [ ] Админ может изменить статус заказа
- [ ] Клиент получает уведомление об изменении статуса
- [ ] Команда /stock показывает товары
- [ ] Остатки товаров уменьшаются после заказа

---

## 🔄 Сброс данных для повторного тестирования

Если хотите начать тестирование заново:

```bash
# Остановить контейнеры
docker compose down -v

# Запустить заново
docker compose up -d

# Применить миграции
docker compose exec app npx prisma migrate deploy

# Заполнить тестовыми данными
npx ts-node prisma/seed.ts

# Обновить telegramId админа (не забудьте!)
docker compose exec postgres psql -U postgres -d veggie_bot -c "UPDATE \"User\" SET \"telegramId\" = 'ваш_id' WHERE role = 'ADMIN';"
```

---

## 📝 Примечания

1. **Polling vs Webhook**: Сейчас боты работают в режиме polling (опрашивают Telegram каждые несколько секунд). Это нормально для локального тестирования.

2. **Фото товаров**: В тестовых данных нет фото (photoId = null). Товары будут отображаться только текстом. Для добавления фото нужно загрузить изображения через Telegram API.

3. **Номер телефона**: При первом оформлении заказа бот запросит номер телефона. При последующих заказах этот шаг будет пропущен (номер сохранен в БД).

4. **Сессии**: Все данные корзины и шаги оформления заказа хранятся в PostgreSQL, поэтому при перезапуске приложения состояние сохраняется.
